# Setup

- Install gems:
```bash
$ bundle install
```

- Install yarn packages:
```bash
$ yarn install --check-files
```

- Create database:
```bash
$ rails db:create
```

- Run migrations:
```bash
$ rails db:migrate
```

- Start application:
```bash
$ rails s
```
